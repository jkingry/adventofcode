namespace AdventOfCode.CSharp.Y2021;

using System.Text;
using System.Text.RegularExpressions;

class Day05 : AdventOfCode.CSharp.RobotElf
{
    Regex _dummy2;
    StringBuilder _dummy1;

    public Day05() : base(5) {}

    public override object Part1()
    {
        var n = Input.Select(int.Parse).ToList();

        // foreach (var s in Input)
        // {

        // }

        return -1;
    }

    public override object Part2() 
    {
        return -1;
    }
}